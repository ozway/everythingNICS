shared_bc_sizes = [1];
tuple_space_size=0;

modules = (
{
	function="RC";
	args=("script.r"); 
//Recommend that the only argument is relative path to the R script, E.g. "script.r"
	libtype="static";
	library="libRC.a";
	size=1;
	points=(
	    (())
    );
}
);

